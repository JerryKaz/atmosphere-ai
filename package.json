{
  "name": "atmosphere-ai",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://github.com/JerryKaz/atmosphere-ai.git",
  "type": "module",

  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",

    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },